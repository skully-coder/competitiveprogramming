# Run-Length Encoding

Given a string containing lower case characters only, compress the string using Run-Length Encoding.

### Example:
```
Input: s = "aaaaaaaaaaaaabbc"
Output: "9a4a2b1c"
```

### Constraints:
- string consists of lower case characters only

### Complexities:
O(N) Time
O(N) Space
